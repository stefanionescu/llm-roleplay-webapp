import { CharactersContent } from '@/features/home/ui/components/characters/characters-content';

export const CharactersSection = () => {
    return (
        <div className="mx-0 w-full overflow-hidden px-0">
            <CharactersContent />
        </div>
    );
};
